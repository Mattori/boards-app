<div class="container">

  <form class="well form-horizontal">

    <fieldset>

      {{developers/updating-developer developer=model.developer}}

      <h3>Projets en cours</h3>
      <hr />
      <div class="table-responsive">
      	<table class="table table-hover table-bordered">
          <thead>
            <tr>
              <th class="col-xs-3">Nom</th>
              <th class="col-xs-3">Description</th>
              <th class="col-xs-3">Date de début</th>
              <th class="col-xs-3">Date butoire</th>
            </tr>
          </thead>
          <tbody>
            {{#each model.developer.projects as |proj|}}
              <tr>
                <td>
                  {{proj.name}}
                </td>
                <td>
                  {{proj.description}}
                </td>
                <td>
                  {{proj.startDate}}
                </td>
                <td>
                  {{proj.dueDate}}
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>

    </fieldset>
  </form>
</div>
{{outlet}}
